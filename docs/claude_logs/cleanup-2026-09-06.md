2026-09-06 · branch `sdu/clean-sept` from `sdu/optim-pred` @ `71890bd` · plan: `docs/claude_logs/audit-2026-09-05-optim-pred.md`

# Cleanup log: what changed, what gated it, what was left

## The gate

Two things must hold after every step:

1. **The test suite**, against the recorded baseline: 628 passed, 1 deselected, exit 0
   (`throwaway/2026-09-05/pytest_baseline_cuda.txt`). Tests deleted together with the
   code they pinned are listed per step; nothing else may go red.
2. **Bitwise fingerprints** of the production paths, captured on the untouched code in a
   frozen worktree (`../RL_for_pRNN_base` at `920c8f8`, the audit commit) by
   `throwaway/2026-09-06/fingerprint.py`. Each case runs its preset for two rollouts with
   one spatial-eval event between them and one extra collection, and digests every weight,
   optimizer moment, rollout buffer, RNG stream and the printed eval numbers:

   | case | what it is |
   |---|---|
   | `prod` | `multienv-fast`, `Selected` impassable positions 0 1 2 3 5 6 7 8, CE + focal 5 + MLP + reward normalization: the `multienv.sh` focal arm |
   | `walkable` | `multienv-fast` as it ships: 5 walkable rooms, MSE |
   | `parity` | the accelerated single L-room, MSE |
   | `reference` | the serial single L-room baseline, MSE, default walkable landmarks |

   `prod` was captured twice on the baseline to establish that the GPU path is
   deterministic run to run before any diff is trusted.

## Steps

Every entry names the commit, what the test gate said, and what the fingerprint
diff said. "IDENTICAL" means every digest in every case matched the baseline.

### 1. Correctness fixes - `fd75b77`

Audit items C2, C3, C4, C5, C6, C7, C8, C15 (see the audit for each). New gates:
`tests/test_audit_2026_09_fixes.py` (C4 by simulating a failed `wandb.init`; C5
by training a frozen world model and counting its episode cuts; C6 by running
`collect_eval_rollouts_batched`; C15 for both probe modes),
`tests/test_configs.py` (the rollout-divides-budget and resume-with-graph
refusals, and the divisible half budget the launcher now names),
`tests/test_training_schedule.py` (zero steps for a learner that does not train).
`tests/test_slurm_invocations.py::BINDINGS["BUDGET"]` moved from 21,968 to
21,984: the old binding was the very budget C2 refuses.

Gate: the touched files pass. Fingerprint: IDENTICAL on prod (131 leaves),
walkable (107), parity (107), reference (100); eval lines equal.

### 2. Deletions - `c8bc5fb`

What went is listed in the commit message and in
`compaction-2026-09-06.md`. The full suite ran 627 passed after nine repairs,
every one of which was a test naming something that no longer exists:

| test | why it broke | repair |
|---|---|---|
| `test_golden_setup` x4 compositions | the fixture records every kwarg `PredictivePPOAlgo` receives; five inert ones (`intrinsic`, `k_int`, `noise_mu`, `noise_std`, `loss`) stopped existing | re-pinned `golden_setup_v1.pt`; the comparison before the recapture showed ONLY those 20 `kwargs.*` leaves moving - both weight hashes and the whole schedule identical in all four compositions (ledger in `capture_golden_setup.py`) |
| `test_action_offset::test_boundary_bootstrap_reads_the_new_episode` x2 | passed `noise_std=` to the algo | dropped the kwarg (it was never read) |
| `test_cuda_graph_wm::test_pooled_graphed_bitwise_equals_eager_no_rng` | asserted `adapter._use_graph_wm()` | asserts `adapter.cuda_graph` |
| `test_references_resolve` | `docs/sab_context/open_choices.md` named a perf result under `tests/perf/` | the path follows the move to `throwaway/2026-09-06/perf/` |
| `test_variable_landmarks::test_pairwise_rules_are_unconstrained_below_two_landmarks` | called `min_cell_gap()`, now a property | dropped the parentheses |

Fingerprint: every case differed from the baseline in exactly ONE leaf,
`log_scalars.intrinsic_rewards` absent - the always-zero array the collector
logged for the deleted intrinsic reward. Weights, both optimizers, every rollout
buffer, all three RNG streams and the eval numbers identical. The harness lists
that key under `ACCEPTED_ABSENT` (with the commit that removed it) so later diffs
against the untouched baseline stay strict; with it excluded, IDENTICAL on all
four cases (130/106/106/99 leaves).

### 3. Naming - 5ed54ee

Audit §2: Hydra-era keys in error messages and docstrings replaced by the
dataclass spelling (`collect.rollout_cuda_graph`, `train_prnn.cuda_graph`,
`run.early_stop`, ...); `world_model.device` -> `models/device.py`; the
adapter's `prediction_mses*` are `prediction_errors*` (surprisal under CE, MSE
under MSE - the old name lied under CE); the four action names have one home,
`utils.common.ACTION_NAMES`, and the fourth is `stay_put` everywhere;
`ACModelSR.SR_size` (which hid a +4) is `sr_and_hd_size`, `SR_single` is
`sr_size`; `getattr(acmodel, "with_CV", True)` is `acmodel.with_CV` with the
attribute declared on `ACModel`; the observation-bank cache moved from the
climbing `parents[2] / "data" / "obs_bank"` to `<RL_STORAGE>/obs_bank`
(`default_bank_dir()`, resolved at call time); `wandb_compare` reads its
entity/project from `RunCfg` and takes `--entity` / `--project`; the
`provenance.write` docstring stopped accusing `obs_bank.py` of a race it no
longer has; `train_fast.sh`'s header no longer says the config "does not use
cuda_graph"; the `agent.py` TODO says what the zero start IS.

Left as named, deliberately: `AgentType.AC = "curious"` (it is in every run
name and every provenance record), the three probe seeds (each pins published
numbers), `schedule.py`/`configs.py` mentions of `rl.frames` (they record the
retired word on purpose).

### 4. Offline tools read the run's own config - fbe5e57

Audit C9 and C11, plus C10's tail. Every offline tool that scores a checkpoint
built its own `Config` from flags: `checkpoint_series.run_config` could not
say `positions` at all (an 8-room run scored as rooms 0..4, silently);
`surprisal_timing.main` and `readout_probe.main` re-typed the preset, the
loss and the readout from flags, hard-coded `action_offset=0`, and passed
`--env.source.n` (which `Selected` no longer has, so both were broken by
cleanup 3 until this step); `prediction_figures.plot_run_predictions` rebuilt
the config from the recorded `argv` - which is the REQUESTED config, and is
the launching program's argv, not the training command's, whenever training
is driven in-process (every fingerprint run records the harness's argv).

The record that was already there: `provenance.json["params"]["config"]` is
the EFFECTIVE config, written by `setup_run` after `setup_training` (so a
rounded hidden size is the recorded one), typed with `_type` tags. What was
missing was the inverse of `to_dict`. Now:

- `Config.from_dict` (`configs.py::_from_jsonable`, type-directed from the
  field annotations) inverts `to_dict`; every preset round-trips, and so does
  the production command line with positions, kept landmarks, a checkpoint
  `Path`, focal CE and the MLP readout (tests/test_configs.py).
- `Config.of_run(run_dir)` reads a training run's provenance and rebuilds its
  config. A field the record predates is read as its default and a field
  since removed is dropped - both printed by name, never silent. ONE record
  shape changed meaning rather than merely gaining or losing a field, and it
  is translated: pre-2026-09-06 `Selected` said "the first n" with
  `positions` null (`_current_shape`, tested against that exact shape).
- `utils/checkpoints.py::run_dir_of` maps a checkpoint file - rolling or
  archived under `checkpoints/` (`ARCHIVE_DIRNAME`, now one home with
  `training/loop.py`) - to the directory holding its provenance.
- `surprisal_timing.load_checkpoint` is the ONE loader both probe tools use:
  config from provenance, rooms through the same `resolve_layouts` the
  training loop uses, `action_offset` from the config. `--positions` is now
  an optional SUBSET of the run's own rooms (`rooms_at`; a position the run
  never trained on is an error), which is what the single-room floor in
  docs/focal-loss-2026-08-31.md needs. `--readout` is gone: the record says.
- `checkpoint_series.main` and `plot_run_predictions` use `Config.of_run`;
  the series keeps `--room/--source/--impassable` as the printed fallback for
  runs older than provenance (2026-08-25).

Checked against every local run with provenance (45 of 57 rebuild; the 12
that do not are the 2026-08-31 sweep runs whose budgets the C2 check now
refuses, plus four base-worktree fingerprint runs before the `Selected`
translation, which now rebuild). End to end: `surprisal_timing` on the
published `focal5mlp` s3 checkpoint reproduces docs/readout-probe-2026-08-31.md
(shown 0.675, background ~0.30, recall 0.677/0.633, miss 0.107/0.119 against
the table's 0.675 / ~0.30 / 0.677-0.686 / 0.630-0.633 / 0.107 / 0.12-0.13);
`--positions 0` walks one room and writes the figure; `readout_probe` on two
rooms runs through; `checkpoint_series` and `plot_run_predictions` on a local
8-room run read the config from provenance and score 5/8 rooms as before.

Also in this step: `palette.classes_of` (one home for pixel->class, used by
`error_decomposition` and `readout_probe`); `LocationStats.update`
vectorised; seven superseded golden fixtures deleted (v0-v4 and the eval
`_v1` pair - no test read them; ledger note in `capture_golden.py`);
`Selected` is positions-only with the launcher deriving `positions` from `n`
(C10); `prediction_figures` imports `Int` (four annotations named an
unimported type) and drops a dead `rng`; a dead `X` in `evaluation/task.py`
and a dead `RandomActionAgent` construction in two tests (the constructor
draws no RNG); `.gitignore` follows the perf and fingerprint moves and stops
carrying an orphaned comment.

Not done, deliberately: C14 (routing `ActorCriticAgent.next_SR` through the
adapter) - optional in the audit and outside the gated paths; the comment in
`rl/collect/agent.py` says what the zero start is.

Fingerprint: IDENTICAL against the baseline on all four cases (130/106/106/99
leaves, with the one accepted-absent key from step 2); eval lines equal.

### 5. The legacy reward alignment retired; goldens pin the live paths - 724bbfa

Audit §1 and §3 on `reward_alignment`. The config had defaulted to `next_obs`
since the dataclass config existed, and no config, launcher or provenance
record (grep over every local `provenance.json`: none says "legacy") ever
selected "legacy"; it survived as `PredictivePPOAlgo`'s and `RolloutConfig`'s
own default - reachable only by constructing the algo directly, which is what
`tests/golden/capture_golden.py` did, so the serial golden pinned a reward
convention no run had trained under. Removed: `RewardAlignment`,
`TrainPolicyCfg.reward_alignment`, `REWARD_ALIGNMENTS`, the two "legacy"
defaults, `compute_curious_rewards`' `alignment` argument, and every
pass-through (`training/setup.py`, `evaluation/on_policy.py`,
`tests/small_config.py`, `test_device_collector.py`'s parametrisation,
`test_action_offset.py`). ONE home remains: `rewards.py::REWARD_TARGET_OFFSET`
(a[i] is rewarded with prediction row i+1), read by the device and the serial
curiosity paths. The adapter keeps `target_offset` because the unshifted pass
(`target_offset=0`) is the oracle tests/test_reward_alignment.py checks the
shift against; its docstrings say so instead of calling it "legacy".
`check/config_keys.py` lists `rl.reward_alignment` under GONE;
docs/prnn-io-alignment.md names the constant where it named the enum.

Goldens, MEASURED before recapturing (throwaway scratch, numbers in the two
capture scripts' ledgers):

| fixture | leaves moved | why |
|---|---|---|
| `golden_v5.pt` -> `golden_v6.pt` (serial training, 2 updates) | 85: round 0's curious_rewards, advantages, policy_loss, value_loss, grad_norm; everything in round 1 and both state dicts | the reward convention changed; round 0's rollout (actions, log_probs, values, SRs, locs) is bitwise unchanged because the collect precedes the first update |
| `golden_eval_v2.pt` -> `golden_eval_v3.pt` (offset 0, eval from reference weights) | 3: `rollout.curious_rewards`, `prnn_loss` (its mean), `SWdist` 0.037325 -> 0.037201 | the reward pass runs one extra noisy step per segment and the sleep simulation behind SWdist draws from the same torch stream afterwards (`rng=None`); actions, SRs, locs, mi_policy, sRSA and every SI value are bitwise unchanged |
| `golden_eval_offset1_v2.pt` -> `golden_eval_offset1_v3.pt` | 0 | that circuit always rewarded a[i] with row i+1 |
| `golden_setup_v1.pt` re-pinned (third time; ledger in `capture_golden_setup.py`) | 4: `<composition>.kwargs.reward_alignment: 'next_obs' -> '<absent>'` in each of the four compositions | the kwarg stopped existing; both weight hashes and the whole schedule identical |

The `_v2`/`v5` files are deleted; `test_golden_eval` no longer asserts a
`reward_alignment` meta key. The retired `multienv` preset stays (it is what
`slurm/train_fast.sh` launches); the README example that used it now uses
`multienv-fast`.

Fingerprint: IDENTICAL against the baseline on all four cases (130/106/106/99
leaves); eval lines equal. Expected, and the point: the gated paths all
rewarded a[i] with row i+1 already, so retiring the switch moved nothing they
compute.

### 6. Prose that had drifted - 681b829 (with step 7)

README: the launch example used the RETIRED `multienv` preset (it still
exists for `slurm/train_fast.sh`, but a reader copying the example would get
the retired budget) - now `multienv-fast`; the preset list was a parenthesis
of names that had drifted from `PRESETS` - now it points at `PRESETS` and
`main_train.py --help`; "`PRNNAdapter` is the only module that imports
`prnn`" was false (`grep -rl "from prnn" curious_george`: construction, the
fork's spatial metrics, the checkpoint helpers, the offline tools) - now it
says what the adapter IS the boundary for and names who else imports the fork
and why. `.env.example` (and the untracked
local `.env`) named `status.pt` as a derived filename; the filenames have one
home in `utils/checkpoints.py` and `status.pt` is the pre-2026-08-28
fallback. The audit's remaining prose items (`docs/prnn-io-alignment.md`'s
table row naming the alignment map) went with step 5.

Left alone, on purpose: `throwaway/count_params.py`, `throwaway/survey_params.py`
(untracked scratch that predates this work) and the untracked `data/` -
the observation-bank cache at its PRE-cleanup-3 location (294 files as of
this commit) that nothing reads any more. It is the user's to delete;
`rm -r data` is safe.

### 7. Final gate - 681b829

Full pytest gate, the baseline's invocation (`CG_DEVICE=cuda uv run pytest -q`,
`-m 'not slow'` from pyproject):

| | passed | failed | deselected | wall |
|---|---|---|---|---|
| baseline, audit commit 920c8f8 (`throwaway/2026-09-05/pytest_baseline_cuda.txt`) | 628 | 0 | 1 | 4:14 |
| first run on the cleanup tree | 646 | 1 | 1 | 4:20 |
| final (`throwaway/2026-09-06/pytest_final_cuda.txt`) | 647 | 0 | 1 | 4:11 |

The one failure was `test_references_resolve::test_every_referenced_path_is_tracked`:
docs/invalid-runs.md named `tests/golden/golden_v1.pt` by path, a fixture step 4
deleted. The record now names the fixture without a path and points at the
version ledger in `capture_golden.py`; nothing else moved between the two runs.
The count rose from 628 because the cleanup added tests (the audit fixes,
config round-trips, the probe tools, zero-step schedules, the launcher
binding) and removed the ones whose subject no longer exists (async envs, the
alignment-switch stubs, one `test_device_collector` parametrisation).

Final fingerprint: step 5's (`throwaway/2026-09-06/fp/step5`, log
`fp_step5.log`) - no code changed after it, steps 6 and 7 touched prose and
records only. IDENTICAL to the baseline on prod, walkable, parity and
reference. The baseline worktree `../RL_for_pRNN_base` is removed with this
commit; its digests under `throwaway/2026-09-06/fp/base1` and `base2` are
what any later comparison reads.

Not pushed. What the user runs next: fresh LRoom-standard and
multienv-impassable jobs from `sdu/clean-sept` (the launchers fetch
`origin/<branch>`, so the branch has to be pushed first, and their `branch`
argument defaults to another branch), compared against the most recent runs
of each.

### 8. Fresh runs against the most recent runs - aea0ebf

The goal's last clause. Not on the cluster (an outward submission the user
did not ask me to make) and not a push: the two arms trained on the local
RTX 4060, which reproduces the L40S curves at matched env steps (memory
`local-4060-reproduces-cluster`, validated 2026-08-26), from the EXACT
config of the most recent finished run of each arm - rebuilt from that run's
own record (`Config.of_run` for a local provenance, `Config.from_dict` on the
wandb config record otherwise; `throwaway/2026-09-07/rerun.py`), seed and
budget included - and compared with `curious_george.check.wandb_compare` on
matched ENV STEPS, each metric against the reference's own adjacent-sample
band. One seed per arm, on different hardware from the reference, so the
claim is "inside the band", never "equal".

The most recent runs, read off wandb (`api.runs(..., order="-created_at")`):

| arm | reference (wandb, finished) | trained | fresh run |
|---|---|---|---|
| single L-room, standard landmarks, `parity` preset (MSE, whitened, entropy 0.035, 43,936 WM steps, seed 2) | `parity-s2-mseB_curious_26-08-31-01-32-31` (curious-george) | Mila L40S, 2026-08-31 | `clean-sept-parity-s2-mseB_curious_26-09-07-04-51-25`, 25 min |
| 8 selected rooms, impassable, focal-5 CE, MLP readout, reward-normalised (the production arm; seed 2) | `mx-impassable-n8-s2-focal5mlp_curious_26-08-31-23-18-20` (curious-george, misfiled - see docs/focal-loss-2026-08-31.md) | Mila L40S, 2026-08-31 | `clean-sept-mx-impassable-n8-s2-focal5mlp_curious_26-09-07-05-16-40` (curious-george-multienv), 21 min |

The 2026-09-01 `mx-mixedcount` run is more recent than the impassable
reference but is a different design (per-room `keep_landmarks`), so it is not
the arm the goal names.

**Single L-room** (`throwaway/2026-09-07/compare_parity.txt`, JSON beside it;
8 matched points per metric, band = the reference's own adjacent-sample std):

| metric | band | points inside | out-of-band deltas |
|---|---|---|---|
| pRNN loss | 0.0022 | 8/8 | - (0.0109 ref vs 0.0119 run at 90M) |
| cur_reward_mean | 0.0023 | 8/8 | - |
| SWdist_onPolicy | 0.121 | 8/8 | - |
| mean SI_onPolicy | 0.247 | 8/8 | - |
| sRSA_onPolicy | 0.125 | 6/8 | +0.18 at 11M, -0.24 at 54M (where the reference itself spikes to 0.81 between 0.57 and 0.69) |
| MI_policy | 0.008 | 5/8 | +0.036, -0.021, +0.019 |
| loc_entropy | 0.101 | 4/8 | -0.32 at 45M, then +0.11, +0.21, +0.28 at 56-79M, back inside at 90M |
| policy_entropy | 0.011 | 4/8 | -0.05, +0.013, +0.030, -0.012 |

51 of 64 matched points inside the band (the 2026-08-26 validation of this
box against the cluster scored 35 of 40). The world-model side is
indistinguishable from the reference at every point; every out-of-band point
is a policy-behaviour metric, small in magnitude and of no consistent sign.
The one pattern a second seed would have to rule out: `loc_entropy` sits
above the reference at three consecutive late points (the fresh policy spread
its visits a little wider from 56M to 79M steps) before rejoining it at 90M.
Inferred, not confirmed: that is seed-plus-hardware noise on a metric whose
band is 0.10 and whose reference moved by 0.41 between its own samples.

**8 rooms, impassable, focal-5 MLP** (`throwaway/2026-09-07/compare_multienv.txt`,
JSON beside it; 6 matched points per metric):

| metric | band | points inside | out-of-band deltas |
|---|---|---|---|
| multiroom/mean_room_sRSA (the arm's headline) | 0.059 | 6/6 | - (0.796 run vs 0.791 ref at 75M, the last analysis) |
| pRNN loss | 0.020 | 6/6 | - (0.0111 vs 0.0112 at 90M) |
| cur_reward_mean | 2.47 | 6/6 | - |
| multiroom/pooled_sRSA | 0.029 | 3/6 | +0.061, +0.061, +0.031 mid-run, inside at the end |
| multiroom/pooled_SWdist | 0.019 | 2/6 | +0.050, +0.035, +0.035, +0.049; inside at the last two |
| MI_policy | 0.003 | 2/6 | -0.013, -0.006, +0.010, -0.014 |
| loc_entropy | 0.043 | 3/6 | +0.056, -0.047, -0.051 |
| policy_entropy | 0.016 | 1/6 | -0.034, +0.021, -0.025, +0.092, +0.020 |
| exploration/coverage | 0.011 | 4/6 | -0.020, -0.031 |
| exploration/nauc | 0.006 | 2/6 | -0.014, -0.007, -0.018, +0.006 |

35 of 60 inside. Two things stand out before the control below: `SWdist` is
above the reference at 13 of the 14 matched points across BOTH arms, and the
policy-behaviour metrics are outside the within-run band more often than
inside.

**The control that decides it** (`throwaway/2026-09-07/compare_seed_control.txt`):
the reference's own sibling seed, `mx-impassable-n8-s3-focal5mlp` - same
commit, same cluster GPU type, same day - compared against the s2 reference
with the same tool. It sits outside the band at 23 of 48 points: `pooled_SWdist`
deltas -0.024, +0.063, +0.030, `policy_entropy` outside at 5 of 6, and
`mean_room_sRSA` ending 0.05 BELOW the reference where the fresh run ends 0.005
above it. The fresh run is as close to the reference as the reference's own
second seed is, on every metric. The within-run adjacent-sample band is a
band for "did this metric move for a reason other than noise", and between
seeds these policy-side metrics move by more than it; that is the measured
seed-to-seed spread, and the fresh runs are inside it. The SWdist sign
pattern (both signs appear between seeds; one sign appears in these two
single-seed fresh runs) is the one observation a second fresh seed on the
cluster would settle; nothing in the code explains it, see the lineage below.

**Code lineage, so the comparison is honest about what it compares**
(from each run's recorded commit: `provenance.json` for the fresh runs and
the local copy of the multienv reference, wandb's git metadata for the parity
reference):

- 8-room reference `c5dd7d9` -> fingerprint baseline `920c8f8`: the training
  and evaluation path differs in ONE file, `envs/layouts.py` (the mixed-count
  `keep_landmarks` design; inert for `keep_landmarks=None`, which both runs
  have, and `min(..., default=UNCONSTRAINED)` guards that only fire below two
  landmarks). From `920c8f8` to the fresh run's `681b829` every change is the
  fingerprint-gated cleanup. Same `prnn` (852cc7d2) and `minigrid` (22ef960)
  commits on both sides. So this pair is the same computation on different
  hardware, seed held fixed - and the L40S-vs-4060 kernels are what make the
  trajectories diverge.
- single-room reference `af168b51` -> `920c8f8`: 16 source files, including
  `rl/collect/collector.py`, `models/prnn_adapter.py`, `training/loop.py` and
  `evaluation/spatial.py` - the 2026-08-31 audit commit `0d3baf9` and
  `3cb9196` ("the seeded probe no longer leaks into the training RNG
  stream") lie between the two references. The parity preset was not run
  again after 01:32 on 2026-08-31, so its most recent run predates those two
  fixes, and the fresh run is the first single-room run on the corrected
  pipeline. That its world-model curve is inside the band at every point and
  51 of 64 points overall says the fixes did not move the curves beyond
  seed-and-hardware noise; it is not a same-code replication and is not
  claimed as one.
- The fresh runs' provenance records `rl-for-prnn` as dirty: the working tree
  held this step's edit to `check/wandb_compare.py` (a full `entity/project/id`
  path for a reference in another project) and the throwaway launcher, neither
  on the training path.

Verdict: everything holds up. Both arms reproduce their most recent run's
headline metric at the end of training, the world-model learning curves are
indistinguishable from the references at every matched point, and every
out-of-band point is a policy-behaviour metric moving by less than the
reference's own second seed does. The fresh runs live in `outputs/` locally
(provenance, checkpoints, archives) and in wandb under the `clean-sept-`
prefix; the comparisons are in `throwaway/2026-09-07/`.

### 9. Cluster reruns against the questions repo's checkpoints - 7c09019, 46012c7

The user's framing, 2026-09-07: the reference run for the cleanup is the run
whose checkpoint the questions repo uses, and the reruns must ALWAYS log
predictions to wandb.

**Which runs those are** (read from `../experiment-curiousgeorge`):

| questions-repo use | checkpoint | run | config |
|---|---|---|---|
| Q3 (object-vector cells): `src/experiments/Q3/collect.py` default, `outputs/cache/Q3_subs.json`, the checkpoint table in `docs/exp_instructions/instructions-Q3.md` | `outputs/fetched/mx-impassable-n8-s2-focal5mlp_curious_26-08-31-23-18-20/predictiveNet_state.pt` | `mx-impassable-n8-s2-focal5mlp_curious_26-08-31-23-18-20` (curious-george, 2026-08-31, L40S) | the production arm: 8 impassable rooms, focal-5 CE, MLP readout, reward-normalised, seed 2, full budget |
| the OMT task, through `CUR_CKPT_DIR` in `.env` (`src/omt/main_task.py` reads it) | `outputs/ckpts/pRNN_curious_26-07-23-10-06-25` | `pRNN_curious_26-07-23-10-06-25` (curious-george, 2026-07-23) | the `reference` preset: its wandb config, translated across the 2026-08-26 key cutover with `check/config_keys.translate_config`, equals the preset on all 33 substantive keys (the 8 "differences" are 0 vs 0.0 and old boolean sentinels) and its 20,480,000 env steps |

The three candidates the user named (`mx-impassable-n8-s3-wm21968-focal2`,
`mx-impassable-n8-s2-swp-lr1e2`, `mx-impassable-n8-s3-wm21968-swp-wd0`) are
half-budget sweep arms from 2026-08-31 and appear nowhere in the questions
repo; the lr 1e-2 arm ended at 0.34 mean room sRSA.

**Predictions in wandb.** The prediction image is the fork's "Observation
Sequence" figure (`plotObservationSequence`, logged as a `wandb.Image`),
drawn by `plotSampleTrajectory` on the plot cadence and gated on
`EvalKind.TRAJECTORY_PLOT`. The `reference` preset has it (every 409,600
steps); `multienv-fast` does NOT, so no multi-room run before today carried
prediction images. This rerun passes `--eval.evals BEHAVIOUR SPATIAL_MULTIROOM
TRAJECTORY_PLOT --eval.plot-every-steps 3333328` (26 figures). Proven before
launch: a two-rollout multi-room run against an offline wandb store wrote two
"Observation Sequence" images. Not made a preset default: the figure rolls
the agent through the env and draws from the RNG streams, so a run with
figures is not bitwise comparable to one without, and the fingerprint
baseline was captured without.

**Multi-room rerun**: `mx-impassable-n8-s2-focal5mlp-clean_curious_26-09-07-22-41-46`
(curious-george-multienv, job 10702442, 23 min on an L40S), against the Q3
reference on matched env steps (`throwaway/2026-09-07/compare_focal5mlp_clean_cluster.txt`):

| metric | band | inside | note |
|---|---|---|---|
| multiroom/mean_room_sRSA | 0.059 | 6/6 | 0.784 vs 0.791 at the last evaluation |
| pRNN loss | 0.020 | 5/6 | the one outside is the FIRST point, 0.024 lower; 0.0123 vs 0.0112 at 90M |
| multiroom/pooled_SWdist | 0.019 | 4/6 | +0.032 and -0.034, no consistent sign |
| per-room SWdist (rooms 0-4) | 0.02-0.12 | mixed | rooms 1-2 above the reference throughout, room 3 below; the reference's own sibling seed moves room 0 by +0.20 and room 3 by +/-0.08 between points (`compare_seed_control` with per-room metrics), so excursions of this size are two seeds |
| 23 of 54 matched points outside overall | | | the sibling seed scores 23 of 48 |

Predictions logged: yes, 26 "Observation Sequence" images (`predictions_logged=True`
from the summary keys). This is the third rerun of this config on the cleanup
tree (local GPU, width runs' 500-wide sibling, this one) and the third time the
world-model curve and the headline sRSA land on the reference.

**Single-room rerun** (`reference` preset, seed 2, the original CPU launcher
`slurm/train_prnn.sh`, wandb project `curious-george`): submitted as job
10702441 and stalled - the launcher asks for an EXCLUSIVE sapphire node and
every sapphire node was allocated or mixed, estimated start the next day
12:20. Cancelled and resubmitted as job 10703374 from a copy of the launcher
without `--exclusive` (the script itself is untouched), on `long-cpu`;
`main-cpu` refuses this account's jobs (accounting/QOS policy). A local
re-run on the dev GPU was started and killed after a minute: the serial path
runs at ~650 env steps/s there, 8.6 h, no faster than the cluster's Milan
nodes, so it was redundant with the queued job.

Result: `pRNN_curious_26-09-08-00-07-29` (curious-george, job 10703374,
4h38m on cn-m001, 1,408 env steps/s; the July original took 4h38m too), 50
"Observation Sequence" images. Against the July run on matched env steps
(`throwaway/2026-09-07/compare_reference_cluster.txt`; 8 points):

| metric | band | inside | what it shows |
|---|---|---|---|
| pRNN loss | 0.0014 | 0/8 | the rerun is HIGHER at every point by a near-constant factor: 0.0296 vs 0.0104 at 2.5M, 0.0124 vs 0.0049 at 20.5M (2.5x) |
| cur_reward_mean | 0.0010 | 0/8 | the same curve (the reward IS the prediction error) |
| SWdist_onPolicy | 0.022 | 0/8 | above at 7 of 8, 0.24 vs 0.13 at the end |
| sRSA_onPolicy | 0.080 | 5/8 | mixed signs; 0.579 vs 0.528 at the end |
| mean SI, MI_policy, loc_entropy, policy_entropy | | mixed | no consistent sign |

This is NOT the cleanup. It is the 2026-08-30 rendering change,
docs/invalid-runs.md "full-colour landmarks": a landmark cell's pixel
separation from floor doubled, "quadrupl[ing] a landmark cell's squared-error
contribution", and the record declares every run and checkpoint before that
line incomparable with anything after on exactly these metrics - pRNN loss,
curiosity-reward scale, sRSA/SI/SWdist. The July run is pale-era; the
fingerprint gate and the 2026-08-31 `parity-s2-mseB` comparison (step 8, the
world-model curve inside the band at every point) are the evidence about the
single-room MSE path on today's rendering. This rerun's own value is the
first `reference`-preset run on the bright rendering, fetched to
`outputs/fetched/pRNN_curious_26-09-08-00-07-29/` (pRNN, policy, provenance;
`Config.of_run` rebuilds it).

🔴 Consequence for the questions repo: `CUR_CKPT_DIR` in `.env` points the
OMT task at `outputs/ckpts/pRNN_curious_26-07-23-10-06-25`, a pale-era
checkpoint. Run on today's code it is fed observations it was never trained
on (every landmark twice as bright, `x` replaced by `triangle3`), and the
repo's own record says its numbers are not comparable. The bright-era
replacement is the fetched run above; repointing `CUR_CKPT_DIR` is the
user's call (the OMT golden fixture is pinned separately and unaffected).

### 10. The single-room arm on the current machinery, on the cluster - (this commit)

The user's correction, 2026-09-09: the single-room reference is the CURRENT
machinery (device backend, 256 streams, pooled world-model steps of 8, all
three CUDA graphs, layer compile, whitened advantages) on the L-room with
MSE - the `parity` preset - not the serial `reference` preset of step 9.
Launched with `slurm/parity.sh '' '' 2 sdu/clean-sept '' mse-clean
--eval.evals BEHAVIOUR SPATIAL_ONPOLICY TRAJECTORY_PLOT --eval.plot-every-steps
3333328` (the preset already carries the trajectory plot every 7.5M steps;
spelled out and densified to 26 images). Job 10723552 was allocated in the
seconds between a status check and the scancel that replaced it, so wandb
holds a crashed stub `parity-s2-mse-clean_curious_26-09-09-12-55-56` with no
data; job 10723582 is the run.

`parity-s2-mse-clean_curious_26-09-09-12-59-04` (curious-george, 28 min on
cn-l005, 92.6k env steps/s, code `46012c7`, 26 images) against the
pre-cleanup `parity-s2-mseB_curious_26-08-31-01-32-31` (code `af168b51`,
same GPU class, same seed and config) on matched env steps
(`throwaway/2026-09-07/compare_parity_clean_cluster.txt`, 8 points):

| metric | band | inside | end (reference vs post-cleanup) |
|---|---|---|---|
| pRNN loss | 0.0022 | 7/8 (the one out by 0.0001) | 0.0109 vs 0.0123 |
| cur_reward_mean | 0.0023 | 8/8 | 0.0116 vs 0.0117 |
| sRSA_onPolicy | 0.125 | 8/8 | 0.652 vs 0.607 |
| mean SI_onPolicy | 0.247 | 8/8 | 0.996 vs 0.974 |
| SWdist_onPolicy | 0.121 | 6/8, but ABOVE at 7 of 8 | 0.106 vs 0.203 |
| MI_policy, loc_entropy, policy_entropy | 0.008 / 0.10 / 0.011 | 4/8, 1/8, 5/8, mixed signs | |

17 of 64 outside overall. The world-model curve and the two spatial
statistics land on the reference; the policy-behaviour metrics wobble as in
every other pair.

**The one-sided SWdist, explained by the repo's own record.** It appeared in
the single-room pairs at both sites (7 of 8 points locally in step 8, 7 of 8
here on the reference's own GPU class) and NOT in the multi-room cluster
pair of step 9 (mixed signs). The difference between those pairs is the
reference's commit: the parity reference `af168b51` predates the two
2026-08-31 commits `3cb9196` and `0d3baf9`, the multi-room reference
`c5dd7d9` postdates them. docs/invalid-runs.md, 2026-08-31, records what
they changed: the seeded probe (`probe_seed=10007`, which the parity
reference DID set) had leaked into the training RNG stream at every analysis
event, and "seeded-probe protocol v2" then made the probe reset `pN.state`
per trajectory and restore state and RNG afterwards - "seeded sRSA/SI values
move; ... different protocols, not regressions". SWdist is computed from the
same probe activity. So the parity reference's on-policy SWdist was
measured under the pre-v2 protocol on a run whose training stream the probe
perturbed, and the post-cleanup run's under v2; the multi-room pair shares
v2 on both sides and shows no offset. That is a pre-cleanup protocol line,
not a cleanup effect: the fingerprint gate holds the cleanup's eval numbers
bitwise against the post-v2 baseline `920c8f8`, SWdist included.

Also launched, 2026-09-09: the MSE arm of the multi-room config (job
10724055, `mx-impassable-n8-s2-mse-clean`, curious-george-multienv: the
focal5mlp-clean line with `--arch-prnn.loss MSE` and no gamma/readout flags,
since `ArchPrnnCfg.__post_init__` refuses a non-linear readout and a focal
gamma under MSE). Result to follow.
