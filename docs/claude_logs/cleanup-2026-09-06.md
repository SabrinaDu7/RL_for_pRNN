2026-09-06 · branch `sdu/clean-sept` from `sdu/optim-pred` @ `71890bd` · plan: `docs/claude_logs/audit-2026-09-05-optim-pred.md`

# Cleanup log: what changed, what gated it, what was left

## The gate

Two things must hold after every step:

1. **The test suite**, against the recorded baseline: 628 passed, 1 deselected, exit 0
   (`throwaway/2026-09-05/pytest_baseline_cuda.txt`). Tests deleted together with the
   code they pinned are listed per step; nothing else may go red.
2. **Bitwise fingerprints** of the production paths, captured on the untouched code in a
   frozen worktree (`../RL_for_pRNN_base` at `920c8f8`, the audit commit) by
   `throwaway/2026-09-06/fingerprint.py`. Each case runs its preset for two rollouts with
   one spatial-eval event between them and one extra collection, and digests every weight,
   optimizer moment, rollout buffer, RNG stream and the printed eval numbers:

   | case | what it is |
   |---|---|
   | `prod` | `multienv-fast`, `Selected` impassable positions 0 1 2 3 5 6 7 8, CE + focal 5 + MLP + reward normalization: the `multienv.sh` focal arm |
   | `walkable` | `multienv-fast` as it ships: 5 walkable rooms, MSE |
   | `parity` | the accelerated single L-room, MSE |
   | `reference` | the serial single L-room baseline, MSE, default walkable landmarks |

   `prod` was captured twice on the baseline to establish that the GPU path is
   deterministic run to run before any diff is trusted.

## Steps

Every entry names the commit, what the test gate said, and what the fingerprint
diff said. "IDENTICAL" means every digest in every case matched the baseline.

### 1. Correctness fixes - `fd75b77`

Audit items C2, C3, C4, C5, C6, C7, C8, C15 (see the audit for each). New gates:
`tests/test_audit_2026_09_fixes.py` (C4 by simulating a failed `wandb.init`; C5
by training a frozen world model and counting its episode cuts; C6 by running
`collect_eval_rollouts_batched`; C15 for both probe modes),
`tests/test_configs.py` (the rollout-divides-budget and resume-with-graph
refusals, and the divisible half budget the launcher now names),
`tests/test_training_schedule.py` (zero steps for a learner that does not train).
`tests/test_slurm_invocations.py::BINDINGS["BUDGET"]` moved from 21,968 to
21,984: the old binding was the very budget C2 refuses.

Gate: the touched files pass. Fingerprint: IDENTICAL on prod (131 leaves),
walkable (107), parity (107), reference (100); eval lines equal.

### 2. Deletions - `c8bc5fb`

What went is listed in the commit message and in
`compaction-2026-09-06.md`. The full suite ran 627 passed after nine repairs,
every one of which was a test naming something that no longer exists:

| test | why it broke | repair |
|---|---|---|
| `test_golden_setup` x4 compositions | the fixture records every kwarg `PredictivePPOAlgo` receives; five inert ones (`intrinsic`, `k_int`, `noise_mu`, `noise_std`, `loss`) stopped existing | re-pinned `golden_setup_v1.pt`; the comparison before the recapture showed ONLY those 20 `kwargs.*` leaves moving - both weight hashes and the whole schedule identical in all four compositions (ledger in `capture_golden_setup.py`) |
| `test_action_offset::test_boundary_bootstrap_reads_the_new_episode` x2 | passed `noise_std=` to the algo | dropped the kwarg (it was never read) |
| `test_cuda_graph_wm::test_pooled_graphed_bitwise_equals_eager_no_rng` | asserted `adapter._use_graph_wm()` | asserts `adapter.cuda_graph` |
| `test_references_resolve` | `docs/sab_context/open_choices.md` named a perf result under `tests/perf/` | the path follows the move to `throwaway/2026-09-06/perf/` |
| `test_variable_landmarks::test_pairwise_rules_are_unconstrained_below_two_landmarks` | called `min_cell_gap()`, now a property | dropped the parentheses |

Fingerprint: every case differed from the baseline in exactly ONE leaf,
`log_scalars.intrinsic_rewards` absent - the always-zero array the collector
logged for the deleted intrinsic reward. Weights, both optimizers, every rollout
buffer, all three RNG streams and the eval numbers identical. The harness lists
that key under `ACCEPTED_ABSENT` (with the commit that removed it) so later diffs
against the untouched baseline stay strict; with it excluded, IDENTICAL on all
four cases (130/106/106/99 leaves).

### 3. Naming - (this commit)

Audit §2: Hydra-era keys in error messages and docstrings replaced by the
dataclass spelling (`collect.rollout_cuda_graph`, `train_prnn.cuda_graph`,
`run.early_stop`, ...); `world_model.device` -> `models/device.py`; the
adapter's `prediction_mses*` are `prediction_errors*` (surprisal under CE, MSE
under MSE - the old name lied under CE); the four action names have one home,
`utils.common.ACTION_NAMES`, and the fourth is `stay_put` everywhere;
`ACModelSR.SR_size` (which hid a +4) is `sr_and_hd_size`, `SR_single` is
`sr_size`; `getattr(acmodel, "with_CV", True)` is `acmodel.with_CV` with the
attribute declared on `ACModel`; the observation-bank cache moved from the
climbing `parents[2] / "data" / "obs_bank"` to `<RL_STORAGE>/obs_bank`
(`default_bank_dir()`, resolved at call time); `wandb_compare` reads its
entity/project from `RunCfg` and takes `--entity` / `--project`; the
`provenance.write` docstring stopped accusing `obs_bank.py` of a race it no
longer has; `train_fast.sh`'s header no longer says the config "does not use
cuda_graph"; the `agent.py` TODO says what the zero start IS.

Left as named, deliberately: `AgentType.AC = "curious"` (it is in every run
name and every provenance record), the three probe seeds (each pins published
numbers), `schedule.py`/`configs.py` mentions of `rl.frames` (they record the
retired word on purpose).
