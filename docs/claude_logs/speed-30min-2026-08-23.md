2026-08-23 · branch `sdu/speed-30min`

# Getting a full training run under 30 minutes with CUDA graphs

**Goal.** 20,480,000 environment steps in **under 30 minutes**, with sRSA,
SWdist, `pRNN loss`, `MI_policy`, `loc_entropy` and `mean SI_onPolicy` inside
the error band of `pRNN_curious_perf_optimized_26-07-08-16-04-37`.

**Method note the user set, and it governs everything below:** short runs do not
settle this. A graphed run has to go to **completion** before its metrics can be
believed - a previous attempt blew up late. No arm is called good on a partial
curve.

## The reference

`pRNN_curious_perf_optimized_26-07-08-16-04-37` (wandb run id
`pRNN_curious_26-07-08-16-04-37`, state `killed`, `_runtime` **63,840 s =
17.7 h**). Config read from wandb, not from memory:

```
env       MiniGrid-LRoom-v0, env_subroom_size 8, door_poss [1,5,3,4], seed 2
exp       num_envs 1, curious_agent, onpolicy_prnn_eval, offpolicy_prnn_eval FALSE
rl        ppo, frames 2048, ppo_batch_size 256, ppo_epochs 4, entropy_coef 0,
          lr 3e-4, discount 0.98, gae_lambda 0.95, reward_alignment next_obs,
          steps 20,480,000, trajs_total 80,000, value_loss_coef 1, k_curious 1
predNet   thRNN_5win, hiddensize 500, seqdur 256, lr 3e-3, dropout 0.15,
          noisestd 0.05, weight_decay 3e-3, sparsity 0.5, ntimescale 2
logging   analysis_interval 200, plot_interval 200, log_interval 1
```

`reward_alignment: next_obs` is the repo default (`Configs/main.yaml:7` ->
`rewards: curious_next_obs`), so it needs no override. **The reference has no
off-policy eval**, so `sRSA_offPolicy` / `SWdist_offPolicy` can be produced in
the new run but have nothing to compare against in that one.

### Matching its two ratios at num_envs=128

The reference is `num_envs=1`; going fast means batching, which silently moves
both gradient-steps-per-experience ratios. To reproduce them at `num_envs=128`
(`frames=32768`):

```
                    reference          matched at num_envs=128
world model    1 step / 256 env steps   predNet.wm_pool_group=1
policy         1 step /  64 env steps   rl.ppo_batch_size=256
```

That combination is arm C of the 2x2 run on 2026-08-22 (job 10448387), which
took **2:31:10** for the same 20.48M steps. So the starting point is 2h31 and
the target is 30 min: **5.0x still to find.**

## Budget, measured

`tests/perf/benchmark.py --updates 12 --warmup-updates 3 --sync-stages`,
production config, RTX 4060. Both flags matter: their defaults (off, 0)
misattributed `collect/curious_rewards` as 29.3% when it is really 1.3%.

```
stage                     eager ms/upd   graphed   speedup   share
update/wm_train                    938       203     4.62x   48.4%
update/policy                      509       522     0.97x   26.3%
collect/sr_step                    196       208     0.94x   10.1%
collect/policy_fwd                 197       209     0.95x   10.2%
collect/env_step                    63        66     0.97x    3.3%
collect/curious_rewards             26        27     0.95x    1.3%
collect/gae                          9         9     0.99x    0.5%
TOTAL                             1939      1244     1.56x
```

Committed already on this branch (`2b66522`): the world-model graph, worth
**1.47x** on fps end to end. What remains is everything the graph does not yet
cover.

## Attempt log

(appended as each is tried - what was attempted, what happened, what it cost)

### 1. Graph the pooled world-model step — WORKED (committed `2b66522`)

The graph had only ever served `train_on_episode`, so the shipped regime ran
ungraphed. `_GraphWMTrainer` now serves `train_on_episodes_batched` as well,
with `batched` and `B` in the graph key.

```
compile_cell  graphed   ms/step        (group=8 L=256 h=500, RTX 4060)
false         false      199.83
false         true        22.28    8.97x
layer         false       56.43    3.54x
layer         true        13.48    4.19x on top of compile
```

Gated bitwise with dropout and noise off: N sequential replays move the weights
identically to N eager `trainStep(batched=True)` calls, `max|d| 0`. Also gated:
every replay advances the weights (a graph replaying at frozen weights would
pass a one-step check), pooled and serial do not share a key, and a ragged
final group captures its own.

### 2. Make `on_device` address-preserving — THE UNLOCK (committed `e3c7c15`)

This is what made graphing usable at all, rather than merely fast.

`Module.to()` REPLACES `param.data`, so the cuda->cpu->cuda round trip the
spatial eval and the sample-trajectory figure sit inside left every captured
graph writing to freed memory. The old mitigation was to skip those
diagnostics under `cuda_graph` - meaning **a graphed run logged no sRSA/SWdist
at all**, which makes the metric comparison this goal is defined by impossible.

`on_device` now holds the original storages alive and copies values back into
them, so every parameter ends at the `data_ptr()` it started with. The
accelerator memory is never released and re-acquired either, so there is no
fragmentation for a re-capture to land in - which was the residual crash cause
that address DETECTION alone could not fix.

Verified in the combination that used to crash: a real training loop with
graphs on ran 8 consecutive on- and off-policy spatial evals, logging every
one. `_skip_model_move_diag` and its two tests are gone; the invariant now
lives in `on_device` and has its own test.

### 3. Graph the PPO minibatch step — WORKED (committed `2c83318`)

`update/policy` was the largest remaining stage: 2005 ms of a 3420 ms update at
`ppo_batch_size=256` (512 gradient steps on a tiny actor-critic, ~3.6 ms each
of nearly pure dispatch).

**What blocked capture, found by bisecting the region in separate processes:**
`torch.distributions.Categorical` validates its arguments by default, and
validation runs `if not valid.all()` on a device tensor - a host sync, which is
illegal during capture. With it off, every stage captures: forward, entropy,
loss, backward, `clip_grad_norm_`, capturable Adam step. Disabled around
capture only; replay runs no Python. Its eager cost is nil (3.558 ms validated
vs 3.752 unvalidated - noise).

Two address problems the world model did not have, both solved by mirroring
into static buffers: `exps` is rebuilt every update, and the minibatch indices
change every step.

**Measured: 3.558 -> 0.275 ms/step, 12.9x.**

**A wrong premise I had to correct.** The first bitwise test compared against
DEFAULT Adam and failed at 2.98e-08. Capture forces `capturable=True`, which
computes Adam's bias correction on-device in a different float32 order. The fix
was to prove that rather than loosen the tolerance: give the eager arm the same
capturable optimizer and it is exactly bitwise, with a separate test pinning
what capturable Adam alone costs.

### Combined effect, synchronized profile (RTX 4060, ppo_batch_size=256)

```
stage                     eager     +wm   +both
update/wm_train             925     218     203
update/policy              2005    2043     180
collect/sr_step             195     209     207
collect/policy_fwd          198     210     208
collect/env_step             63      66      66
collect/curious_rewards      26      27      26
TOTAL ms/update            3420    2781     898
fps                        9014   10922   29232
                                   1.21x   3.24x
```

Applied to the 2x2's measured arm times: g8/p256's 51:42 -> **~16 min**,
g1/p256's 2:31:10 -> proportionally more since the world model dominates there.
**Both are projections until a full-length run says otherwise**, which is the
whole point of the method note at the top.

### Cluster arms in flight (all 20.48M steps, single L-room, ent=0, seed 2, p256)

```
10449716  pool=8  eager             control
10449715  pool=8  wm graph
10449732  pool=8  wm + policy graph
10449718  pool=1  eager             control
10449717  pool=1  wm graph
10449733  pool=1  wm + policy graph
```

### 4. The bottleneck moved: it is now the spatial eval, not training

Measured with both graphs on, warm, synchronized (`--updates 6
--warmup-updates 3 --sync-stages --include-analysis`), RTX 4060:

```
training stages          870 ms/update  ->  625 updates =  9.1 min
analysis/spatial        53.8 s /event   ->  100 events  = 89.7 min
```

**Graphing worked so well that the training loop is now 9% of the run.** Every
further optimization of the update loop is worth at most a few minutes; the
spatial eval is worth an hour.

`N_ANALYSIS` is therefore now an argument (`slurm/train_fast.sh` $11). Setting
it to **50 for a 20.48M-step run is not a corner cut - it is exactly the
density the reference ran at**: `analysis_interval: 200` updates at
`frames: 2048` is one event per 409,600 env steps, which over 20,480,000 steps
is 50. The 100 default was inherited from a 102.4M-step preset.

⚠️ 53.8 s is one warm call on a dev box whose CPU is weaker than a cluster
node's, and the eval runs on CPU by construction (`evaluation/spatial.py` wraps
it in `on_device(modules, "cpu")` because the analysis is numpy/pynapple).
Deriving from the cluster instead: 10449716 (pool=8, eager) was 34% done at
22:57 with 30 events, projecting ~67 min and ~89 events, against ~36 min of
eager training compute - so ~21 s per event there. Either way it dominates once
the loop is graphed.

**Not pursued, and why:** moving the eval's rollout to GPU would be the real
fix, but it changes the device the metric is computed on, and this goal is
defined by the metrics not moving. Reducing the cadence to the reference's own
density changes nothing about how any single number is measured.

### Two dead ends worth recording

- **Distribution validation in the rollout.** Since `Categorical` validation
  was what blocked graph capture, the obvious next thought was that its host
  sync also costs in the 256-iteration rollout. Measured: rollout 742 ms
  validated vs 740 ms unvalidated. **No cost.** It blocks capture without
  costing time.
- **Fusing the curiosity forward into the training pass** (proposed earlier
  from an unsynchronized profile that put it at 29.3%). Synchronized it is
  **1.3%**, 26 ms/update. Dead on payoff, before even reaching the semantics
  objection that it would collapse `wm_pool_group`'s sequential steps.
