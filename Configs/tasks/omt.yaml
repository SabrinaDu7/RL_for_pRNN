defaults:
    - _self_  

room_size: 16

training:
  num_trajs: 5000 # trajectories to train on in novel env
  saving_interval: 50 # batches between saving
  analysis_interval: 25 # batches between logging
  lr_trials: 2 
  lrgroups: [0, 1, 2]
  resetOptimizer: False
  continueTraining: False

testing:
  timesteps: 1000 # timesteps for test trial
  control_location: [4, 7]
  whichPhase: 0 # which phase to analyze in quantifyObjectLearning